{
  "version": 2,
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/node",
      "config": { "distDir": "dist/selfie" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}
